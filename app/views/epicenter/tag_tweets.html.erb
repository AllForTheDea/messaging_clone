<h1><%= @tag.phrase %></h1>

<% @tag.tweets.order(created_at: :desc) do |tweets| %>
	<div class ="well">
		<p><%= link_to tweet.user.username, show_user_path(id: tweet.user_id) %></p>
		<p><%= tweer.message.html_safe %></p>
	</div>